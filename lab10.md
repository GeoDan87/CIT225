# Lab 10
1. In the `hfh.newsletter_subscription` drop the `supporter_id` column. In the `hfh.podcast_subscription` table drop the `language_code` column. Open git bash, cd to the CIT225 folder and run `git pull origin main`. Open an anaconda prompt window and navigate to your CIT225 folder. Once in the CIT225 directory run the following code `python insert_generated_data_lab10.py`.
</br>
</br>

2. Query the `hfh.podcast_info`, `hfh.newsletter_info`, `hfh.newsletter_subscription` and `hfh.podcast_subscription` tables and familiarize yourself with the data in those tables. 
</br>
</br>

3. The marketing department of Habitat for Huge Manatees wants to know how many subscribers there are for each newsletter that are both active and inactive. The newsletter name should be human readable. In the space below, draw out what you would want your results set to look like.
</br>
</br>
</br>
</br>
</br>
</br>
</br>
</br>
</br>
</br>
</br>
</br>
</br>

4. By hand write a query that would produce the results that you drew out in question 3. 
</br>
</br>
</br>
</br>
</br>
</br>
</br>
</br>
</br>
</br>
</br>

5. Type out the query in HeidiSQL and debug it until it runs.
</br>
</br>

6. Copy the query from question 5 into a new tab and alter the query so that you get sums for each grouping and the subtotals for each newsletter regardless of the status. Write below which extension of group by you used.
</br>
</br>
</br>

7. Copy the query from question 6 into a new and alter the query so that you get the sums for each grouping and subtotals for newsletters and status. Write below which extension of the group by you used.
</br>
</br>
</br>